# # Remove duplicate/conflicting settings
# sonar.projectName=earendel  # ✅ Keep only essential config
# sonar.projectVersion=1.0
# sonar.projectBaseDir=${github.workspace}

# # Simplify path configuration
# sonar.sources=backend
# sonar.cs.opencover.reportsPaths=${github.workspace}/CoverageReports/coverage.opencover.xml
